file(
	GLOB_RECURSE 
	GLOB
	CONFIGURE_DEPENDS 
	"${CMAKE_CURRENT_SOURCE_DIR}*.cpp" 
	"${CMAKE_CURRENT_SOURCE_DIR}*.c"
)

add_library(Unity STATIC ${GLOB})
target_include_directories(Unity PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")